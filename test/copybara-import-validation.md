# Copybara import validation

Throwaway file used once to validate the reverse import flow (issue #132): a pull request
opened on the public mirror should produce a corresponding review pull request on the private
canonical repo. Delete after the check.
